version: "3.5"
services:
  api:
    image: joesweeny/statshub_api:$RELEASE
    ports:
      - "80:8000"
    environment:
      NAME: "Joe"

  console:
    image: joesweeny/statshub_console:$RELEASE
    environment:
      NAME: "Joe"
    tty: true